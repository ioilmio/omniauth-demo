<p id="notice"><%= notice %></p>

<h1>Posts</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.user_id %></td>
        <td><%= post.content %></td>
        <td><%= link_to 'Show', post %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

        <%=link_to '<a>Logout</a>', destroy_user_session_path, method: :delete%>
           <%= link_to '<a>New Post</a>', new_post_path %>
        
            <%=link_to "<a>Sign in with Github</a>", user_github_omniauth_authorize_path%>
            <%=link_to "<a>Sign up</a>", new_user_registration_path%>